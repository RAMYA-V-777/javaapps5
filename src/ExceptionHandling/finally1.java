/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package ExceptionHandling;

/**
 *
 * @author velmurugan
 */
public class finally1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        /*finally(block)->return ur resources safely to the system,it is used->to close all ur prgrms properly also Nested try block
         /*finally(block)->return ur resources safely to the system,it is used->to close all ur prgrms properly also
        Nested try block
        final       finally      finalize
        final->keyword->var
        method
        class(with some restrictions)
        finally->used in exception handling->is a block
        finalize()->is a method->present in obj class->want to clean unreferenced object->automatically do garbage collection->clean up memory
        throw->explicitly throwing an exception->used in user defined exception/costom exception/customize exception
        syntax:
        throw new ArithmeticExpression();//inbuilt class exceptions
        throws->
        returntype methodname () throws IOException,SQLException
        {
        //try,catch or try(throw),catch->throw should be caught by try
        }
        
        
        
        user defined exception:
        -------->to satisfy our need
        
        */
       /* final       finally      finalize
        final->keyword->var
        method
        class(with some restrictions)
        finally->used in exception handling->is a block
        finalize()->is a method->present in obj class->want to clean unreferenced object->automatically do garbage collection->clean up memory
        throw->explicitly throwing an exception->used in user defined exception/costom exception/customize exception
        syntax:
        throw new ArithmeticExpression();//inbuilt class exceptions
        throws->
        returntype methodname () throws IOException,SQLException
        {
        //try,catch or try(throw),catch->throw should be caught by try
        }
        
        
        
        user defined exception:
        -------->to satisfy our need
        
        */
    }
    
}
