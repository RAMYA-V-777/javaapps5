/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package InputOutputStreams;

/**
 *
 * @author velmurugan
 */
public class BufferredStream1 {
/*
  used with file/datainputstream,we can add buffer in any prgrm by(because ->instead of taking the data directly,frst the data will me moved to buffer and from buffer we can read the content from the buffer)
    DataInputStream ds=new DataInputStream(new BufferredInputStream(System.in));
    IN FILE:
    BufferredInputStream bis=new BufferredInputStream(new FileInputStream("a.txt"));
    Note:After using any Stream we have to close it
*/
    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
    }
    
}
